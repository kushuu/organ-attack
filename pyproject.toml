[project]
name = "organ-attack"
version = "0.1.0"
description = "A multiplayer card game where players attack opponents' organs and defend their own"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=11.0.0",
]
